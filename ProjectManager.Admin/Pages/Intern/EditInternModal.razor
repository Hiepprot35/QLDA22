@inherits InternModalEditBase

@if (@isLoading)
{
    <TemplatedLoadingPopup />
}
<RadzenTemplateForm TItem="Intern" Data="@editModel" Submit="@OnSubmit" InvalidSubmit="@OnInvalidSubmit">
    <div class="row">
        <div class="col-sm-6 padding-right-0">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Mã thực tập" /><span class="required">*</span>
                <RadzenTextBox Name="ClassName" @bind-Value="@editModel.ID_Intern" />
                <RadzenRequiredValidator Component="ClassName" Text="Thông tin bắt buộc" />
            </div>
        </div>
        <div class="col-sm-6 padding-right-0">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Tên nơi thực tập" /><span class="required">*</span>
                <RadzenTextBox Name="ClassName" @bind-Value="@editModel.Name" />
                <RadzenRequiredValidator Component="ClassName" Text="Thông tin bắt buộc" />
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Sinh viên" />
                <RadzenDropDown class="radzen-dropdown"
                                AllowClear="true"
                                TValue="long?"
                                FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                                FilterOperator="StringFilterOperator.Contains"
                                AllowFiltering="true"
                                Data="@listStudent"
                                TextProperty="Name"
                                ValueProperty="Id"
                                Placeholder="--Tất cả--"
                                @bind-Value="@editModel.StudentId" />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6 padding-right-0">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Giảng viên" />
                <RadzenDropDown class="radzen-dropdown"
                                AllowClear="true"
                                TValue="long?"
                                FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                                FilterOperator="StringFilterOperator.Contains"
                                AllowFiltering="true"
                                Data="@listTeacher"
                                TextProperty="Name"
                                ValueProperty="Id"
                                Placeholder="--Tất cả--"
                                @bind-Value="@editModel.TeacherId" />
            </div>
        </div>
        
    </div>
    <div class="row">
        <div class="col-sm-6 padding-right-0">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Điểm" /><span class="required">*</span>
                <RadzenTextBox Name="ClassName" @bind-Value="@editModel.Point" />
                <RadzenRequiredValidator Component="ClassName" Text="Thông tin bắt buộc" />
            </div>
        </div>
        <div class="col-sm-6 padding-right-0">
            <div class="form-group">
                <RadzenLabel class="form-label" Text="Mô tả" /><span class="required">*</span>
                <RadzenTextBox Name="ClassName" @bind-Value="@editModel.LinkDownload" />
                <RadzenRequiredValidator Component="ClassName" Text="Thông tin bắt buộc" />
            </div>
        </div>

    </div>
    @if (isShow)
    {
        <div class="row">
            <div class="col-sm-6 padding-right-0">
                <div class="form-group">
                    <RadzenLabel class="form-label" Text="Người tạo" />
                    <RadzenTextBox @bind-Value="@editModel.CreatedBy" Disabled="true" />
                </div>
            </div>
            <div class="col-sm-6">
                <div class="form-group">
                    <RadzenLabel class="form-label" Text="Ngày tạo" />
                    <RadzenDatePicker class="radzen-date-picker" @bind-Value="@editModel.CreatedDate" DateFormat="@Constants.DateFormat" Disabled="true" />
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6 padding-right-0">
                <div class="form-group">
                    <RadzenLabel class="form-label" Text="Người sửa" />
                    <RadzenTextBox @bind-Value="@editModel.ModifiedBy" Disabled="true" />
                </div>
            </div>
            <div class="col-sm-6">
                <div class="form-group">
                    <RadzenLabel class="form-label" Text="Ngày sửa" />
                    <RadzenDatePicker class="radzen-date-picker" @bind-Value="@editModel.ModifiedDate" DateFormat="@Constants.DateFormat" Disabled="true" />
                </div>
            </div>
        </div>
    }

    <div class="modal-footer">
        <RadzenButton class="btn btn-danger" Click="@Cancel">
            <RadzenIcon Icon="close" class="material-icons-custom" />Đóng
        </RadzenButton>
        <RadzenButton type="submit" class="btn btn-primary">
            <RadzenIcon Icon="save" class="material-icons-custom" />Lưu
        </RadzenButton>
    </div>
</RadzenTemplateForm>